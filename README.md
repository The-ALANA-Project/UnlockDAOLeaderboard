
  # Unlock DAO Leaderboard

  This is a code bundle for Unlock DAO Leaderboard. The original project is available at https://www.figma.com/design/sWx1wurLHiqEquDOmxpjsk/Unlock-DAO-Leaderboard.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  